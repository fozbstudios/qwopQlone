put points as childen on the far ends of each limb 
on kepress or ai cmd:
    apply (impulse or force?)
        else cancel movement
on physics tick
    calculate angle between points (vector math?)
    if angle isnt within bounds
        cancel rotation of physicically lower limb
