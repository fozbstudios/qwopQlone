extends CollisionShape2D

# class member variables go here, for example:
# var a = 2
# var b = "textvar"

func _ready():
	# Called when the node is added to the scene for the first time.
	# Initialization here
	# print("l")
	# print(self.transform.origin)
	# print(self.transform.x)
	# print(self.transform.y)
	# print("ln")
	# print(self.transform.origin.normalized())
	# print(self.transform.x.normalized())
	# print(self.transform.y.normalized())
	# print("g")
	# print(self.to_global(transform.origin))
	# print(self.to_global(transform.x))
	# print(self.to_global(transform.y))
	# print("gn")
	# print(self.to_global(transform.origin).normalized())
	# print(self.to_global(transform.x).normalized())
	# print(self.to_global(transform.y).normalized())
 pass

#func _process(delta):
#	# Called every frame. Delta is time since last frame.
#	# Update game logic here.
#	pass
